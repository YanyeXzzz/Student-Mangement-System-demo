import directive from 'element-ui/packages/loading/src/directive';
import service from 'element-ui/packages/loading/src';

export default {
  install(Vue) {
    Vue.use(directive);
    Vue.prototype.$loading = service;
  },
  directive,
  service
};
